<div class="navbar">
  <div class="navbar-inner">
    <a class="brand" href="#">Jen's Really Bad App</a>
    <ul class="nav">
      <li class="active"><a href="#">Home</a></li>
  	</ul>
    <ul class="nav">
	  <li class="pull-right"><%= current_user.email %></li>
    </ul>
  </div>
</div>

<h1>Welcome!</h1>
<% if user_signed_in? %>
<p> You are signed in as <%= current_user.email %>.
<p><%= link_to("Logout", destroy_user_session_path, :method => :delete) %>
<% else %>
<p><%= link_to("Register", new_user_registration_path) %>
<p><%= link_to("Login", user_session_path) %>
<% end %>